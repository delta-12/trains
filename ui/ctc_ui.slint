import { HorizontalBox } from "std-widgets.slint";

export component CtcUi inherits Window {
    title: "CTC";
    HorizontalBox {
        padding: 50px;
        Text {
            text: "This is the CTC UI.";
        }
    }
}
