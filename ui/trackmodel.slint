import { HorizontalBox } from "std-widgets.slint";

export component TrackModelUi {
    HorizontalBox {
        padding: 50px;
        Text {
            text: "This is the Track Model UI.";
        }
    }
}
