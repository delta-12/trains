import { HorizontalBox } from "std-widgets.slint";

export component TrainControllerUi {
    HorizontalBox {
        padding: 50px;
        Text {
            text: "This is the Train Controller UI.";
        }
    }
}
