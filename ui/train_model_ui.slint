import { HorizontalBox } from "std-widgets.slint";

export component TrainModelUi inherits Window {
    title: "Train Model";
    HorizontalBox {
        padding: 50px;
        Text {
            text: "This is the Train Model UI.";
        }
    }
}
