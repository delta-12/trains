import { HorizontalBox } from "std-widgets.slint";

export component TrainModelUi {
    HorizontalBox {
        padding: 50px;
        Text {
            text: "This is the Train Model UI.";
        }
    }
}
