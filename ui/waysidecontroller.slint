import { HorizontalBox } from "std-widgets.slint";

export component WaysideControllerUi {
    HorizontalBox {
        padding: 50px;
        Text {
            text: "This is the Wayside Controller UI.";
        }
    }
}
