import { HorizontalBox } from "std-widgets.slint";

export component TrackModelUi inherits Window {
    title: "Track Model";
    HorizontalBox {
        padding: 50px;
        Text {
            text: "This is the Track Model UI.";
        }
    }
}
