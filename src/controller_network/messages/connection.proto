syntax = "proto3";

package controller_messages;

enum ControllerType
{
    CONTROLLER_TYPE_WAYSIDE = 0;
    CONTROLLER_TYPE_TRAIN = 1;
}

message Connection {
    uint32 controller_id = 1;
    ControllerType controller_type = 2;
}